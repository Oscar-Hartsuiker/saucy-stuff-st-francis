---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
const base = import.meta.env.BASE_URL;
const posts = (await getCollection('blog')).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

---

<Layout title="Blog - Oscar">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12" id="main-content">
    <h1 class="text-4xl font-bold text-center mb-12 text-gray-900 dark:text-gray-100">Blog Posts</h1>

    <section aria-label="Blog post listing">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {posts.map((post) => (
          <article class="blog-card bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden flex flex-col h-full transition-all duration-200 hover:-translate-y-1 hover:shadow-lg" aria-labelledby={`post-title-${post.id}`}>
            {post.data.heroImage && (
              <picture>
            <source srcset={`{post.data.heroImage.replace(/^\//, '').replace(/\.(png|jpg|jpeg)$/, '.webp')}`} 
                  type="image">
                <img
                  src={post.data.heroImage}
                  class="w-full mx-auto object-cover"
                  alt={`Featured image for ${post.data.title}`}
                  loading="lazy"
                  width="720"
                  height="360"
                />
              </picture>
            )}
            <div class="p-6 flex flex-col flex-grow">
              <h2 id={`post-title-${post.id}`} class="text-xl font-semibold mb-3 text-gray-900 dark:text-gray-100">{post.data.title}</h2>
              <p class="text-gray-600 dark:text-gray-400 mb-4 flex-grow">{post.data.description}</p>
              <div class="mt-auto">
                <time datetime={post.data.pubDate.toISOString()} class="text-gray-500 dark:text-gray-400 text-sm block mb-2">
                  {post.data.pubDate.toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  })}
                </time>
                <div class="text-right">
                  <a
                    href={`${base}blog/${post.id}`}
                    class="inline-block px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors duration-200"
                    aria-label={`Read full article: ${post.data.title}`}
                  >
                    Read More
                  </a>
                </div>
              </div>
            </div>
          </article>
        ))}
      </div>
    </section>
  </div>
</Layout>
